# Tool Repair

Use TOOL-REPAIR MODE only after a structured failure. Reproduce the failure without PHI, create a new version under generated_tools/<tool_name>/<version>/, add a manifest, tests, synthetic fixture, limitations, and registry entry, and request independent artifact review. Do not overwrite prior versions or alter stable medical thresholds or equations.
