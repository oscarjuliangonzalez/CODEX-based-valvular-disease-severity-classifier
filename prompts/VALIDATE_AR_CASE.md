# Validate AR Case

Validate runs/<case_id> without changing measurement values. Check every agent result JSON, schema conformance, artifact paths, masks, contours, measurement lines, spectral envelopes, calibration, source provenance, threshold application, and safety language. Require indeterminate if unsupported conclusions remain.
