# JSON Contracts

Schemas live under schemas/: case manifest, agent task, agent result, mask artifact, measurement result, evidence result, final report, tool manifest, and tool evolution. Run python scripts/validate_json_outputs.py <json> <schema> to validate artifacts.
