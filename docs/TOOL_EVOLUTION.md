# Tool Evolution

A specialist agent may request adaptive-toolsmith after a reproducible structured failure. New adapters must live under generated_tools/<tool_name>/<version>/, include a manifest, tests, synthetic fixture, limitations, and registry update, and must never overwrite a prior validated version or alter ASE thresholds.
