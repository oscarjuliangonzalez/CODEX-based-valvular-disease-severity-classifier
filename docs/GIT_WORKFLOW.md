# Git Workflow

Work on feature branches, preserve user changes, avoid destructive Git commands, inspect staged content before commits, and never commit PHI, secrets, patient studies, model weights, or generated case outputs. Required verification is pytest and python scripts/smoke_test.py.
